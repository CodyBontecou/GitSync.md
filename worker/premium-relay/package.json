{
  "name": "gitsync-premium-relay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types",
    "types:check": "wrangler types --check",
    "migrate:local": "wrangler d1 migrations apply premium-relay --local",
    "dry-run": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3",
    "@cloudflare/vitest-pool-workers": "^0.21.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.122.0"
  }
}

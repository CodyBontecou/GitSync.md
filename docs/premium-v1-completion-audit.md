# Premium v1 / GitSync Assist completion audit

Audit date: 2026-08-13. This is a prompt-to-artifact checklist, not a release certificate.

## Concrete success criteria

1. Preserve all paid-up-front/manual local Git, Shortcuts, and callbacks outside the subscription.
2. Offer optional monthly/annual StoreKit entitlement, purchase/restore/manage UI, and use verified current entitlements—not cache or event DTOs—as access authority.
3. Implement GitHub App push webhook → Cloudflare D1 transactional outbox → Queue → minimal silent APNs fan-out, with installation-scoped authorization and fail-closed Apple verification.
4. On wake/foreground, perform only a clean pull fast-forward on the selected checked-out branch; never automatically stage, commit, rebase, merge, resolve, force-push, or push.
5. Support per-repository enrollment, branch/network/power policy, foreground reconciliation, health/attention states, and multi-device routing.
6. Keep repository contents, local paths, and Git credentials device-to-provider; store only disclosed minimal relay metadata.
7. Supply accurate privacy/terms/App Privacy/setup/retention/deletion/monitoring/kill-switch/rollback/App Review documentation.
8. Verify code locally and then verify authorized deployed services plus signed physical-device scenarios before release.

## Prompt-to-artifact checklist

| Requirement / gate | Artifact or command evidence | Status |
|---|---|---|
| Existing manual Git remains outside Premium | Manual UI/action paths in `VaultView.swift`, `GitControlSheet.swift`, Shortcuts, and callback handler have no entitlement gate; `README.md` and terms state the boundary | Implemented; regression suite green |
| Monthly/annual products in one group | `PremiumProductIdentifiers`; local-only `Sync.md/GitSyncAssist.storekit` wired into shared Debug test/launch scheme but excluded from target resources; XCTest verifies source JSON group/product/period agreement and Release build proves it is absent from the app bundle; current read-only ASC JSON captures `/tmp/gitsync-asc-subscription-groups-current.json` and `/tmp/gitsync-asc-subscriptions-current.json` | Local monthly/annual StoreKit testing configuration implemented and accepted by review `a31eadae`; current authenticated App Store Connect inventory still has zero groups/subscriptions, so live provisioning remains outstanding |
| Purchase, restore, manage, state UI | `PremiumStorefront.swift`, `PremiumSettingsView.swift`, `AppSettingsView.swift` | Implemented; sandbox/live purchase evidence missing |
| Exact StoreKit transaction finish | `PremiumFinishableTransaction` wraps the delivered StoreKit `Transaction`; event path finishes it then refreshes | Implemented and unit-tested |
| `currentEntitlements` is authority; cache cannot grant | `PremiumEntitlementStore.refresh`, generation guard, `cachedVerifiedProof` comment/use; event-without-current-entitlement test | Implemented and unit-tested |
| Relay disabled means no request | unresolved `PREMIUM_RELAY_BASE_URL` is rejected by `PremiumAPIConfiguration`; transport test confirms zero calls | Implemented/fail-closed |
| Debug/Release APNs capability | exact XCTest verifies `remote-notification`, unresolved relay fail-closed behavior, Debug/Release `APS_ENVIRONMENT`, both entitlement assignments, and `$(APS_ENVIRONMENT)` declaration (`/tmp/gitsync-release-config-test-2.log`); read-only live `asc bundle-ids` capability/profile inventory | Code config is regression-tested, but live bundle ID `YVCND75XTL` exposes only `IN_APP_PURCHASE` (no Push Notifications), only two App Store profiles exist (no development profile), and device build failed with “No Accounts” / “No profiles” (`/tmp/gitsync-device-signing-probe.log`) |
| Cold silent-push startup and bounded completion | `PremiumNotificationBridge`, `PremiumRuntime.processPush`; completion-gate concurrency plus timeout/cancellation and pre-timeout-success tests | Implemented and deterministically tested; physical-device timing evidence missing |
| Installation-scoped bearer registration/deletion | StoreKit purchase uses the stable installation UUID as `appAccountToken`; the UUID is mirrored to device-only Keychain and restored after UserDefaults loss/reinstall so deletion credentials and signed identity remain reachable; relay requires exact signed match and unique original-transaction ownership; all issued installation deletion capabilities remain valid until one purge consumes them together, so concurrent reauthorization cannot invalidate an in-flight delete; capability insertion is conditioned on the same live installation deletion generation as session issuance, preventing a delayed authorization from adding a post-purge key; hashed receipts for every completed issued capability make restart retry idempotent whichever credential reached Keychain; server expiration can reactivate and re-enroll a retained repository tuple on newer signed renewal while preserving its opaque channel/outbox foreign keys; explicit user deletion writes reinstall-durable `pending`/`completed` Keychain state and remains terminal | Implemented; replay, ownership, reinstall identity recovery, concurrent capability issuance/old-capability purge, delayed authorization vs purge, alternate-credential lost-response retry, expiration→renewal→re-enrollment with retained outbox, explicit purge, stale-link, and stale-write tests pass; injected Keychain failure tests prove pending-state creation failure cannot reach the relay, credential replacement failure cannot erase an existing item, and completed-state update failure preserves pending across defaults loss/reinstall; cross-device restore remains relay-ineligible unless Apple signs that installation token |
| APNs token replacement/delete safety | Runtime persists monotonic registration generations for token and every configuration snapshot in both UserDefaults and device-only Keychain, restoring the maximum after defaults loss/reinstall; relay accepts only current/newer generations, rechecks token/generation on registration delete and stale APNs invalid-token tombstones, and rechecks enrollment liveness in the same batch before inserting device-channel mappings; deterministic delayed stale-commit, equal-generation configuration, registration-vs-unenrollment, delete/invalid-token TOCTOU, in-flight deletion, inactive restart, defaults-loss generation restore, and restart-retry tests | Implemented and locally race-tested; live rotation evidence missing |
| Push payload contains only wake/routing data | `PremiumSilentPush.parse`; Worker `silentPayload` exact `aps`, `channel`, `hint`; relay test asserts no repository field | Implemented and tested |
| GitHub HMAC before parse | `worker/premium-relay/src/github.ts` and `src/index.ts`; signature rejection tests; signed `installation/deleted` uninstall handling authenticates the raw body before parsing, atomically removes device-channel routing and tombstones enrollment/link state, and remains effective while new push admission is kill-switched | Implemented and tested, including uninstall then relink/re-enrollment recovery |
| Push-only event/repository/branch validation and dedupe | Worker allowlist (including standard `base_ref`), delivery/outbox uniqueness, branch match | Implemented; live GitHub delivery evidence missing |
| D1 transactional outbox and autonomous recovery | migration schema, immediate `dispatchPendingOutbox`, scheduled global dispatcher | Implemented and tested locally |
| Queue multi-device fan-out | Worker consumer joins device channels and per-device environment; tests cover sandbox/production and one GitHub push routing to two independently entitled installations/tokens | Implemented and deterministically tested; live multi-device APNs evidence missing |
| Concurrent Queue duplicate side-effect guard | `outbox_claims` lease table/claim around APNs request; concurrent duplicate Queue test asserts one APNs request | Implemented and deterministically tested |
| APNs invalid-token vs provider/config failures | `classifyApns`; only token-specific errors tombstone; token/generation guards preserve concurrent replacements; config/provider errors retry to DLQ | Implemented and race-tested |
| StoreKit verifier fail-closed | `worker/storekit-verifier`: Apple's official `SignedDataVerifier`, pinned Apple roots, chain/purpose/bundle/environment/App Apple ID validation, online checks, strict DTO, and fixed-category/status-only error logging; 12 Workers-runtime tests including signed transaction/notification/renewal-info chain fixtures and JWS marker redaction from message/name/cause; service binding in relay | Implemented and locally validated; signed renewal info supplies bounded billing-grace expiry and must agree with transaction identity; logging correction accepted by review `2338bcb2`; deployment and live Apple Sandbox/Production evidence missing |
| App Store notification dedupe/order | pending UUID, supported type/state consistency checks, entitlement/access mutations, and processed marker are one D1 batch transaction; endpoint tests cover signed `TEST` and transactionless `RENEWAL_EXTENSION` summary acceptance without entitlement mutation, cryptographically verified billing-grace renewal info extending relay access, unsupported/mismatched types, pending retry, duplicate, stale revocation, expiration cleanup, and newer revocation tombstones | Implemented and deterministically tested |
| Pull-only clean fast-forward | `RepositoryPullRunner` → `executePullOnly`; fresh branch/ancestry/worktree checks; SAFE checkout only, no FORCE in this path; a libgit2 reference transaction locks `HEAD` and the checked-out branch from exact expected-OID revalidation through checkout/index mutation and branch-ref commit; real-libgit2 fixtures inject both an external worktree write and a concurrent same-branch `A→C` advance, proving bytes/commit are preserved and automation fails closed, then verify the same transaction completes a clean `A→B` fast-forward | Implemented and deterministically race-tested; correction accepted by `c05b7a5a` |
| Never automatic stage/commit/rebase/merge/resolve/push | Automated coordinator call graph reaches only `executePullOnly`; spy counters assert zero stage, local commit, commit/push, rebase/continue, merge/finalize, push-current, tag-push, and conflict-resolution calls; manual APIs remain separate | Source-inspected and deterministically call-graph tested |
| Dirty/diverged/missing/wrong/auth/trust/unavailable attention | typed `RepositoryPullResult`, `RepoAssistHealth`, coordinator mapping, vault/list/settings UI; deterministic matrix covers local changes, divergence, remote branch missing, authentication, wrong branch, unavailable repository, and generic failure while preserving prior success metadata | Implemented and unit-tested; physical-device/provider failures remain in release matrix |
| Per-repository branch/network/power controls | `RepoAssistSettings`, repository settings UI, coordinator gates | Implemented and persistence-tested |
| Foreground reconciliation | `Sync_mdApp` scene-active task and runtime | Implemented; device evidence missing |
| Repository contents/path/credentials never sent to relay | narrow Swift DTOs, Worker schema/payload/logs, API metadata test | Source/test verified locally |
| Privacy and terms | `site/privacy.html`, `site/terms.html`; app links use existing production URLs `https://gitsyncmd.app/privacy.html` and `/terms.html` | Production URLs resolve (latest read-only fetch `msrdmv52eqbdj4`), but the live privacy policy still says the app has no backend/analytics/device identifiers and the live terms still describe only a $9.99 one-time purchase/local client; publishing current files and post-publish hash/content verification remain blocked |
| App Privacy inventory and manifest | `docs/premium-v1-app-privacy.md`; bundled `Sync.md/PrivacyInfo.xcprivacy` declares no tracking, required-reason API use, first-party onboarding analytics, and Assist identifier/purchase/enrollment/diagnostic categories; local privacy policy separately discloses iOS onboarding analytics and the site's conditional Cloudflare Web Analytics beacon/dimensions | Locally bundle-verified (`/tmp/gitsync-privacy-manifest-build-2.log`) with exact manifest regression test (`/tmp/gitsync-privacy-storekit-focused-tests.log`); app inventory correction accepted by review `83bcb2fe`, website disclosure by `998d6799`, and private data-request flow by `037f2b6a`; App Store Connect answers, production analytics retention/deletion execution, published policy, and archive privacy report still not verified |
| Setup/operations/App Review | `docs/premium-v1-release-runbook.md`, `docs/premium-v1-release-evidence-template.md`, Worker READMEs | Complete local instructions plus a redacted evidence-capture matrix; live execution remains blocked |
| Retention/deletion/monitoring/kill switch/rollback | Worker cron/purge plus both runbooks; authenticated in-app Assist deletion; user-reviewed private data-request email draft supplies separate opaque onboarding/Assist installation IDs without credentials; focused test `/tmp/gitsync-privacy-request-test.log` | Implemented/documented locally; private-flow correction accepted by review `037f2b6a`; production deletion secret/support execution/provider-layer deletion and live drill remain missing |
| iOS 17 / Swift 5 | project build settings and simulator build | Verified |
| iOS unit gate | signed simulator `xcodebuild test`; `.github/workflows/xctest.yml` preserves simulator ad-hoc signing because Keychain-backed Premium deletion tests require entitlements | Current run: 143 unit tests plus 1 UI smoke test, 0 failures (`/tmp/gitsync-final-all-current-49.log`), including isolated in-memory identity migration, APNs generation restore, delayed enrollment response vs deletion, and injected pending/credential/completed Keychain failure windows; exact privacy correction accepted by review `d743e088`, CI signing by `a529262f`, and deletion-state update-in-place correction by `30f6f6d4` |
| iOS build gate | generic iOS Simulator build-for-testing plus Release simulator artifact inspection; `.github/workflows/xctest.yml` now rebuilds Release on every CI run, requires a lintable root `PrivacyInfo.xcprivacy`, and fails if any `.storekit` file ships | Current Release CI-equivalent audit succeeded after the final pull-reference transaction correction (`/tmp/gitsync-final-release-49.log`), the bundled manifest is byte-identical to source, local StoreKit config is absent, correction review `ee340db7` accepted the underlying resource/scheme/test configuration, and workflow review `a192c1c2` accepted the durable gate; GitHub-hosted workflow evidence still requires an authorized commit/push |
| plist/entitlement and diff gates | `plutil -lint` and `git diff --check` | Passed |
| Worker tests/typecheck/generated types | Local commands plus `.github/workflows/premium-workers.yml` using Node 24, locked `npm ci`, tests/typecheck/generated-types/dry-run for Premium relay, private verifier, and the app's existing onboarding analytics Worker; verifier startup check and fresh local migrations; no deploy/remote/provision command | Current full matrix: relay 40 tests plus typecheck/types/dry-run/migrations (`/tmp/gitsync-final-premium-relay-44.log`), verifier 12 tests plus typecheck/types/dry-run/startup (`/tmp/gitsync-final-storekit-verifier-44.log`), onboarding analytics 10 tests plus typecheck/types/dry-run/migration (`/tmp/gitsync-final-onboarding-analytics-44.log`); original Premium-only workflow accepted by review `39704669`, but expanded workflow has no GitHub run until committed/pushed |
| Fresh local D1 migrations | Premium relay `migrate:local`; onboarding analytics `db:migrate:local` | Current fresh state: relay applies 33-command `0001` then 2-command `0002_github_link_consumption_nonce.sql` (`/tmp/gitsync-final-premium-relay-44.log`), preserving an upgrade path for databases that already recorded `0001`; analytics executes 7 commands with `CI=1` (`/tmp/gitsync-final-onboarding-analytics-44.log`); retention/deletion/CI hardening accepted by review `54687588` |
| Worker deployment validation | local dry-runs/startup; current read-only authenticated Wrangler 4.122 inventory (`/tmp/gitsync-cf-d1-current.json`, `/tmp/gitsync-cf-queues-current.txt`, deployment error captures); onboarding deployment/version/secret/schema captures | Relay dry-run passed (`/tmp/relay-all-issued-deletion-receipts-dryrun.log`); current live account still has no `premium-relay` D1 or Premium queues and both Premium Workers return Worker-not-found. Existing onboarding analytics is deployed only as the May 30 fetch-only v1 (`/tmp/gitsync-analytics-version-current.json`), has no secrets (`/tmp/gitsync-analytics-secrets-current.txt`), and therefore lacks current cron/deletion hardening despite its D1 schema existing (`/tmp/gitsync-analytics-remote-schema-current.json`) |
| Strict configuration/privacy surface | Central relay configuration gate rejects malformed kill switch/placeholders; kill-switched webhooks still verify bounded body/HMAC/event/delivery before no-write acknowledgement; verifier has `workers_dev: false`; unused GitHub account ID removed; secrets only named in ignored local templates | Implemented/tested locally; production secret manager and routes not configured |
| GitHub App/live webhook | Local GitHub-App link/webhook relay code and tests, including a nonce-owned single-use callback claim that prevents a losing concurrent/stale callback from writing a GitHub installation and raw-HMAC-authenticated `installation/deleted` handling that atomically removes routing even under the admission kill switch; current read-only repo-hook inventory `/tmp/gitsync-gh-hooks-current.json`; `user/installations` authorization probe | Local push/callback/uninstall lifecycle is implemented and relay-tested; repository currently has an existing push hook but its URL/ownership was intentionally not exposed and cannot establish it is the required Premium GitHub App; current OAuth token cannot list GitHub App installations (403), so permissions/callback/install/live signed delivery remain unverified |
| Git/PR state | uncommitted working tree on `main`; zero staged files; no tracked `.pi`/`.pi-subagents` changes | Verified locally; no commit/PR/deploy performed |

## Current missing or weak evidence

The objective is **not complete** and the goal must remain active because release-critical external and physical-device evidence is absent:

- App Store subscription products/group/localizations/prices and App Privacy answers. Authenticated `asc` confirmed zero subscription groups/subscriptions; the only existing app webhook is an unrelated app-version-state webhook, not the Premium App Store Server Notification v2 endpoint.
- Signed Apple provisioning, APNs key/team/topic, sandbox and production token delivery.
- Deployment and live Apple Sandbox/Production evidence for the locally implemented certificate-chain-validating `STOREKIT_VERIFIER`, including online OCSP behavior and current entitlement/server-notification state.
- Authorized Cloudflare D1/Queue/DLQ/Worker resources, real identifiers, secrets, route/domain, migrations, monitoring, and kill-switch/rollback drills.
- Authorized GitHub App credentials, callback/webhook, read-only permission, installation, and real signed delivery.
- Published privacy/terms pages and verified production URLs; deployed first-party onboarding analytics 90-day cron, separate deletion secret, provider-layer retention/logging/backup settings, and installation-scoped deletion drill.
- Physical-device matrix in `premium-v1-release-runbook.md`: locked/background/suspended/force-quit, Low Power/offline, token rotation, multiple devices, external folder, dirty/diverged/wrong branch, auth/trust, LFS, and subscription lifecycle.
- Swift runtime token/delete races (including delayed stale relay commits), bridge timeout/cancellation and exactly-once behavior, a real-libgit2 external-write-before-SAFE-checkout race, concurrent Queue duplicate delivery, and duplicate/out-of-order App Store notification endpoints are covered. Targeted review `95d71760` returned `ACCEPT_LOCAL_IMPLEMENTATION` for the pull-safety seam, race fixture, and forbidden-operation callgraph assertions.
- Narrow review `8413725d` rejected the prior token test because it did not model delayed server commit; generation ordering fixed that. Follow-up `a1cd47bc` identified restart-durability and token-delete TOCTOU gaps; persisted barrier and guarded delete fixed those. Review `78b1684f` then found a terminated process could not retry deletion because its credential was only in memory; a deletion-capable credential is now persisted at authorization and startup retries/clears the barrier, with inactive/restart coverage. Correction review `64e761c6` accepted the initial restart-retry surface. Final broad reviews `b14eb69b`, `b856248f`, and `38aa9dd0` found stale APNs invalid-token tombstoning, kill-switch/config privacy gaps, cross-installation ownership ambiguity, expiry/restart deletion, and overstated notification event guards; current code adds token/generation guards, strict central configuration (including secrets), authenticated no-write kill switch, private verifier config, unique original-transaction ownership, credential restoration, explicit notification type/state validation, and server-driven cleanup on signed expiration. Follow-ups `fd8f40bc` and `16335552` confirmed the earlier fixes but found expiration cleanup and missing-secret validation; both were covered and `fc508725` accepted them. Combined audit `2923f26c` then found lost-response deletion retries and expiration→renewal reactivation gaps; deletion receipts now make purge idempotent, signed renewal can reactivate server-expired installations, and explicit user deletion remains terminal. Correction review `661d0666` accepted that lifecycle surface. Broad review `1d00877d` then found retained unique enrollment rows blocked post-renewal setup; `createEnrollment` now revives only a tombstoned tuple under the current installation generation and `56f3abdf` accepted re-enrollment. Broad review `07be0a4d` then found expired-session deletion, cached server-revoked bearer, and equal-generation configuration races; authorization now returns a separate non-expiring deletion capability, every non-token registration increments the persisted generation, and reauthorization is forced on relevant 401s. Correction `3b16f3bc` found GitHub relink without an APNs token bypassed the registration 401 path; `startGitHubLink` now directly reauthorizes and retries once, with deterministic coverage. Review `d9852747` found its retry raced deletion; consent/barrier checks and a gated test fixed it, and `6351fd28` accepted it. Broad review `fc6bf41f` found re-enrollment channel rotation violated retained outbox foreign keys; revival now preserves the opaque channel and its test includes a dependent outbox row. Correction `a230a5a3` then found the response still returned an unused proposed channel; the endpoint now reads and returns the persisted active channel, the test asserts that exact response, and `76357fb7` returned `ACCEPT_LOCAL_IMPLEMENTATION`. Broad review `4c70b02b` then found each authorization rotated the sole deletion capability, allowing an in-flight older capability to fail before the new response reached Keychain. The relay now retains every issued capability per installation until one successful purge atomically removes all keys, while its receipt preserves idempotency only for the capability actually used; deterministic coverage deletes with the first capability after a second authorization, and `446a6912` returned `ACCEPT_LOCAL_IMPLEMENTATION`. A further composition audit found the new key insert itself needed the same deletion-generation predicate as session issuance; it is now conditional, with an injected delayed-authorization test proving a concurrent purge leaves no session or deletion key, and `abf9698d` returned `ACCEPT_LOCAL_IMPLEMENTATION`. Follow-on restart analysis found a second capability could have reached Keychain even if the first capability performed deletion. Review `625bc59d` correctly rejected the initial schema-only fix because the handler wrote only the used receipt. The purge batch now converts every live capability for that installation into a receipt before deleting live keys, the test proves both actually issued capabilities retry as 204 without synthetic receipt insertion, and `9ef2d2a3` returned `ACCEPT_LOCAL_IMPLEMENTATION`. Broad audit `46d64143` returned `ACCEPT_LOCAL_IMPLEMENTATION_AND_AUDIT` across the then-current iOS, relay, verifier, migration, tests, and documentation. After the privacy-request correction, bounded composition review `21b87dbd` returned `ACCEPT_CURRENT_LOCAL_CORRECTION_AND_AUDIT`. Later full-slice reviews `f184ea4a` and `aa2084b7` found three additional composition gaps: delayed enrollment responses could restore local consent after installation deletion, in-flight device registration could restore a channel mapping after unenrollment, and the relay rejected signed Apple `TEST`/transactionless `RENEWAL_EXTENSION` summary notifications. Current code rechecks local consent/barrier/entitlement/credential before applying enrollment responses, rechecks live enrollment ownership inside the device registration batch, and accepts/deduplicates those two nontransaction notification classes without entitlement mutation; deterministic tests and current 137-test/35-test suites cover the fixes. Targeted correction review `47fb1c45` returned `ACCEPT_FINAL_RACE_AND_NOTIFICATION_CORRECTIONS` for all three fixes and their evidence. Subsequent adversarial review `4aa1a0e1` found three more lifecycle gaps: grace-period `currentEntitlements` could be locally active while relay authorization rejected the expired transaction, reinstall could lose the UserDefaults-only installation UUID needed to reach surviving Keychain deletion state, and a losing concurrent GitHub callback could write a link after its state claim lost. Current code verifies signed renewal info and retains its bounded grace expiry server-side for authorization, mirrors/restores the installation UUID through device-only Keychain, and uses a per-claim callback nonce so only the successful single-use state claimant can link. The callback nonce is added by `0002` rather than rewriting the already-applied baseline migration, and identity/privacy tests inject isolated in-memory Keychain storage so hosted tests never delete or overwrite the app's production identity account. Current 138-test iOS, 38-test relay, 12-test verifier, migrations, dry-runs, and Release artifact audit pass. Targeted review `9ca2f643` returned `ACCEPT_GRACE_REINSTALL_CALLBACK_CORRECTIONS`. Subsequent broad local review `a6e8709f` found create/delete enrollment retries could strand local state after a committed response was lost. Exact active creation retries now return the same persisted opaque channel without relinking a different GitHub installation, and owned tombstone deletion retries return 204 while missing/cross-installation channels remain 404. HTTP-handler tests cover both lost-response paths; relay 39/39 and all gates pass, and targeted review `08f1d57a` returned `ACCEPT_ENROLLMENT_RESPONSE_LOSS_CORRECTIONS`. External release evidence above still blocks overall completion.

A later lifecycle audit (`03110095`) found that APNs registration generation was not reinstall-durable, explicit deletion could become locally reusable after reinstall, and GitHub App uninstall delivery was unhandled. The runtime now mirrors each generation to device-only Keychain and restores the maximum with UserDefaults; deletion uses reinstall-durable terminal `pending`/`completed` state and support-only reuse messaging; and signed GitHub `installation/deleted` events atomically tombstone routing even while push admission is kill-switched. Relay coverage is now 40/40.

Review `90273400` then found that replacing `pending` with `completed` used delete-then-add semantics, so a failed Keychain replacement could erase the only fail-closed marker. `KeychainService.save` now updates an existing item in place and adds only on `errSecItemNotFound`. Follow-up reviews found and closed two adjacent windows: failure to create the first pending marker can no longer reach the relay, and no retry can call the relay without a read-verified pending marker. Injected tests cover pending-state save failure, credential replacement/read failure, and completed-state update failure across defaults loss/reinstall. Focused evidence is `/tmp/gitsync-keychain-failure-window-tests-3.log` (6/6); final targeted review `30f6f6d4` returned `ACCEPT_KEYCHAIN_UPDATE_IN_PLACE_CORRECTION`. Current full signed evidence is `/tmp/gitsync-final-all-current-49.log` (143 unit tests plus 1 UI smoke, zero failures), with Release artifact evidence in `/tmp/gitsync-final-release-49.log`.

Fresh broad local audit `0a67eee9` found one further P1: after ancestry validation, another Git process could advance the checked-out branch and the old unlocked `git_reference_set_target` would overwrite that commit after checkout. The fast-forward mutation now holds libgit2 transaction locks for `HEAD` and `refs/heads/<branch>`, revalidates the exact captured local OID under lock, and commits the ref only through that transaction. A real-libgit2 `A→B` / concurrent `A→C` test proves Assist preserves `C` and aborts without checkout, while a reset fixture proves clean `A→B` still succeeds (`/tmp/gitsync-pull-ref-lock-tests-2.log`). Correction review `c05b7a5a` returned `ACCEPT_PULL_REF_TRANSACTION_CORRECTION`; final broad re-review `9814dec7` returned `ACCEPT_CURRENT_LOCAL_IMPLEMENTATION_AND_AUDIT` with no remaining local blocker while independently retaining every external gate above as incomplete.

No deployment, provisioning, publishing, commit, or PR was performed. The app remains relay-disabled until an explicit HTTPS build setting is authorized.

## 2026-08-16 release execution update

Operator authorized commit/push and external provisioning. Evidence below supersedes the corresponding "missing" items above.

### Repository and CI

- Entire feature tree committed and pushed to `main` (`3fb13ff..47fc59f`: iOS feature, premium workers + CI gate, analytics upgrade, docs/legal, Release-artifact CI gate, verifier deploy fix). Working tree clean; `.pi-subagents/` gitignored.
- **GitHub-hosted Premium Workers workflow passed** (run `31936564104`, relay 40 + verifier 12 + analytics 10 tests, typecheck, generated types, dry-runs, fresh migrations) and Build Number Guard passed (`31936564107`). XCTest workflow run `31936564096` result recorded in the evidence bundle.

### App Store Connect (live, authenticated)

- **Push Notifications capability added** to bundle `bontecou.Sync-md` (`YVCND75XTL`); capability now `IN_APP_PURCHASE` + `PUSH_NOTIFICATIONS`.
- **Subscription group "GitSync Assist"** (`22312794`) created with products `com.bontecou.gitsync.assist.monthly` (subscription `6802000537`, $1.99/mo) and `com.bontecou.gitsync.assist.annual` (`6802000730`, $14.99/yr): en-US display names/descriptions (≤55 chars), 1290×2796 review screenshots uploaded, complete equalized price matrix verified across 175 territories, availability set, `verify_state` completed for both.

### Cloudflare (live, authenticated, account `e4265f322e6380ee832b83ad45e3e8c0`)

- **`storekit-verifier` Worker deployed** (private, service-binding only, `workers_dev: false`). Deploy exposed a real bug no local gate caught: `jsrsasign` module init violates workerd global-scope rules. Fixed by lazy `import()` on first request (commit `47fc59f`); typecheck + 12/12 tests green after fix.
- **`premium-relay` D1 created** (`9a2e6daf-3a8f-40a7-9235-54eaafceeb94`, id committed) and **migrations `0001` + `0002` applied remotely** (33 + 2 commands).
- **Queues created**: `premium-relay-outbox` and `premium-relay-outbox-dlq`.
- **`sync-md-onboarding-analytics` upgraded in production** to the retention/deletion version: cron trigger deployed, `DELETION_TOKEN` secret set (value handed to operator for password manager; also temporarily at `/tmp/onboarding-deletion-token.txt`), remote `onboarding_events` schema verified consistent.

### Published legal pages

- Updated `privacy.html`/`terms.html` deployed to the `syncmd` Cloudflare Pages project and **verified live at `https://gitsyncmd.app/privacy` and `/terms`** (Effective date August 13, 2026; GitSync Assist disclosures present). Pre/post content hashes confirmed the project maps to gitsyncmd.app.

### Still blocked on operator (unchanged)

1. **APNs auth key (.p8) creation** (developer.apple.com only, no API) → then set relay secrets `APNS_PRIVATE_KEY`/`APNS_KEY_ID`/`APNS_TEAM_ID`.
2. **GitHub App creation** (UI only): Contents read-only, `push` webhook only, callback `<relay>/v1/github/callback`, webhook `<relay>/v1/webhooks/github` → then `GITHUB_APP_ID`/`GITHUB_APP_SLUG`/`GITHUB_APP_PRIVATE_KEY`/`GITHUB_WEBHOOK_SECRET` and real `GITHUB_CALLBACK_URL`.
3. **Relay deploy** (after 1–2): real vars, secrets, public URL; then set the **App Store Server Notification v2 endpoint** in ASC to `<relay>/v1/app-store/notifications`.
4. **ASC App Privacy answers** (web UI; no CLI surface) using `docs/premium-v1-app-privacy.md`.
5. **Physical-device release matrix** per runbook.
6. Relay stays intentionally undeployed until secrets exist: strict config fail-closed would reject every request, and a public half-configured endpoint is worse than none.

## 2026-08-16 CI resolution (XCTest on GitHub)

First GitHub-hosted runs of the expanded suite failed with host-app malloc aborts ("pointer being freed was not allocated", constant address, ~5 ms into specific tests). Ruled out in order: StoreKit TestAction sessions (removed from scheme; crash persisted), ad-hoc signing vs entitlements (reproduced both ways), app-code memory errors (local full suite + ASan clean; crashed tests pass in milliseconds in isolation locally).

Deployed an ASan probe run on the failing test (`ci-bisect-nosign` branch, run 31941264861). AddressSanitizer pinpointed the invalid free inside the **iOS 26.2 simruntime's `libswift_Concurrency.dylib`**:

```
#1 swift::TaskLocal::StopLookupScope::~StopLookupScope()
#2 swift_task_deinitOnExecutorImpl(...)
#4 Sync_md.RepoPersistenceStore.__deallocating_deinit
```

i.e., a Swift Concurrency runtime bug in task-local scope teardown during task deinit — our object just happened to be the one being deinitialized. Local runs (iOS 26.5 runtime) never hit it. The previously-green Aug 12 runs predate the persistence store/coordinator code paths that run deinits on tasks.

Fix: XCTest workflow now selects an iPhone from the **newest installed runtime** instead of the first-listed device (commit `e3c772c`; diagnostic branch closed as PR #33 with the evidence). Verified: **XCTest run `31945818950` on `main` — 143 tests, 0 failures, Release artifact audit passed.** Premium Workers and Build Number Guard also green on the same push. All repository CI gates are now evidenced on GitHub-hosted runners.
